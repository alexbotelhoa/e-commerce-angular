type Query {
  userAll: [User!]!
  userById(id: ID!): User
  userByField(fields: userQueryInput!): [User!]!
}

input userQueryInput {
  name: String
  email: String
  cpf: Int
  phone: Int
  level: Int
  hasActive: Boolean
}
