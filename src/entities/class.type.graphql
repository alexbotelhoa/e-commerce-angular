type Class {
    id: ID!
    name: String!
    levelCodeId: ID!
    levelCode: LevelCode!
    institutionId: ID
    periodId: ID
    carrerId: ID
    sessionId: ID
    studentGrades: [ClassStudentGrade!]!
}
