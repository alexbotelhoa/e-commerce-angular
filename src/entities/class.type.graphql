type Class {
    id: ID!
    name: String!
    levelCodeId: ID!
    levelCode: LevelCode!
    institutionId: ID
    periodId: ID
    carrerId: ID
    sessionId: ID
    localId: ID
    campusId: ID
    regionalId: ID
    startDate: DateTime
    endDate: DateTime
    hasEcampus: Boolean
    hasEyoung: Boolean
    hasActivated: Boolean
}
