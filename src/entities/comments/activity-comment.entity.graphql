type ActivityComment implements Comment {
    id: ID!
    text: String!
    userId: ID!
    parentId: ID
    activityId: ID!
    classId: ID!
    replies: [ActivityComment!]!
    user: User!
    createdAt: DateTime!
    viewerCanDelete: Boolean!
    viewerCanEdit: Boolean!
}
