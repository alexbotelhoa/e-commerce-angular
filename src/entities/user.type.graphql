type User {
  id: ID!
  name: String!
  initials: String!
  roles: [Role!]!
  userRoles: [UserRole!]!
  isTeacher: Boolean!
  teacherClasses: [TeacherClass!]!
  totalCompletedActivities: Int!
  totalAvailableActivities: Int!
  onboarded: Boolean!
  defaultLevelTypeId: LevelTypeId!
  avatarId: ID
  avatar: Avatar
}
