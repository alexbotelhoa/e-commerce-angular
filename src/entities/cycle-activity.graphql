type CycleActivity {
    id: ID!
    cycleId: ID!
    activityId: ID!
    order: Int!
    cycle: Cycle!
    activity: ActivityUnion!
    nextActivity: CycleActivity
    previousActivity: CycleActivity
    viewerHasCompleted: Boolean!
}

