type ActivityTimer {
    id: ID!
    userId: ID!
    cycleActivityId: ID!
    startTime: DateTime!
    completionTime: DateTime
    completed: Boolean!
    classId: ID!
    cycleActivity: CycleActivity!
    currentUser: User!
    completedActivities: String!
}
