type EmbeddedActivity implements Activity {
    id: ID!
    name: String!
    description: String
    typeId: ActivityTypeId!
    type: ActivityType!
    data: EmbeddedActivityData!
    active: Boolean!
    estimatedTime: String!
}
