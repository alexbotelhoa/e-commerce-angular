type Mutation {
    completeActivity(data: CompleteActivityInput!): CompleteActivityResult!
}

input CompleteActivityInput {
    cycleActivityId: ID!
    classId: ID!
}

union CompleteActivityResult = ActivityTimer | SimpleError
