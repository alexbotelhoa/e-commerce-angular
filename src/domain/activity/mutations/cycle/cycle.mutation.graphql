type Mutation {
  createCycle(data: CycleData!) : Cycle!
  deactivateCycle(id:ID!) : Cycle!
  activateCycle(id:ID!) : Cycle!
  addActivitiesToCycle(id: ID!, activityIds: [ID!]!, orders: [Int!]) : [CycleActivity!]!
}

input CycleData {
  name: String!
  levelThemeId: ID!
  active: Boolean
}
