type Mutation {
    createLevel(data: CreateLevelInput!): Level!
}

input CreateLevelInput {
    name: String!
    description: String
    typeId: LevelTypeId!
    order: Int! = 0
    active: Boolean!
    codes: [ID!]!
}
