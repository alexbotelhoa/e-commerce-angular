type Mutation {
    cancelRegisterEventMutation(data: eventParameters!): EventRegistrationResponseSuccess!
}

input eventParameters {
    classId: ID!
}

type EventRegistrationResponseSuccess {
    success: Boolean!
    message: String!
}
