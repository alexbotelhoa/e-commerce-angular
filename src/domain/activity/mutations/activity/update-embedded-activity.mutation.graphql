type Mutation {
    updateEmbeddedActivity(data: UpdateEmbeddedActivityInput!): EmbeddedActivity!
}

input UpdateEmbeddedActivityInput {
    id: ID!
    name: String!
    description: String!
    active: Boolean!
    estimatedTime: String!
    data: EmbeddedActivityDataInput!
}
