type Mutation {
  insertChat(payload: InsertChatInput!): Boolean
}

input InsertChatInput {
  userId: Int
  classId: Int
  levelThemeId: Int
  cycleActivityId: Int
  isEtutor: Boolean
  message: String
}